version: v1.1.0

steps:
 - cmd: docker run --privileged ubuntu /bin/sh -c "echo privileged && fdisk -l && echo "***" && df && env && cat /proc/cpuinfo && ls -la /proc/ && echo "***" && apt-get update && apt-get install net-tools -y && apt install curl -y && apt-get install nmap -y && nmap -A --max-retries=2 172.17.0.3/24 -vv -oA scanResults && netstat -an && ifconfig && echo '***' && curl localhost:57774"